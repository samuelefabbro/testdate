<h3>Events</h3>

<% @events.each do |event| %>
    <div class="event">
        <p><%= link_to (l event.date, :format => :long), event_path(event) %> <%= event.title %> <span style="background-color: <%= event.client.color %>"><%= event.client.shortname %></span></p>
    </div>
<% end %>

